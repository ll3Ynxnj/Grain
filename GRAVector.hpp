#ifndef GRAIN_GRAVECTOR_HPP
#define GRAIN_GRAVECTOR_HPP

template <typename T> struct GRAVec2 { T x, y; };
template <typename T> struct GRAVec3 { T x, y, z; };
template <typename T> struct GRAVec4 { T x, y, z, w; };

#endif // GRAIN_GRAVECTOR_HPP
